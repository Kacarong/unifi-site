"""강의자료 PDF + 전사 → 구조 보존 인덱스 → 요청별 요약정리본 PDF.

핵심 규칙(토큰 절약):
  1차  원본(PDF 텍스트 + 전사) 전체를 한 번만 LLM 에 태워 `index.json` 을 만든다.
  이후 요약정리본 요청은 **인덱스만** 입력으로 쓴다. 원본을 통째로 다시 보내지 않는다.
  다만 "3장 예제만" 같은 요청을 위해 섹션 단위 원본 재조회 경로는 열어 둔다
  (한 섹션은 보통 전체의 1/10 이하).
"""
